import React ,{useEffect,useState} from 'react'


const Options=()=>{
  return(
    <h3>Hello from the options component- im am rendered via the SingleScrollItem component which is rendered in ScrollChoices</h3>
  )
}

export default Options;
